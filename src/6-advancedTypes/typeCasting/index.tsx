import React from "react";

const Typecasting = () => {
	// this syntax is only valuable for plain javaScript with Typescript
	// const userInputElement = <HTMLInputElment>document.getElementById("user-input");
	// const userInputElement = document.getElementById("user-input") as HTMLInputElement; //this is a type
	// the "!" says that the input cant return null

	return <></>;
};

export default Typecasting;
